div class="social-buttons"
  a class="g-plus button small expand radius" href="https://plus.google.com/share?url=#{request.original_url}" data-action="share" data-width="500" data-height="400"
    = t("social.share")

  a class="fb-share button small expand radius" href="https://www.facebook.com/sharer/sharer.php?u=#{request.original_url}" data-action="share" data-width="626" data-height="436"
    = t("social.share")

- content_for(:javascript) do
  javascript:
    $("body").on("click", "[data-action = 'share']", function(e) {
      e.preventDefault();
      var url = $(this).attr("href");
      var width = $(this).data("width");
      var height = $(this).data("height");
      var sizeString = "width=" + width + ",height=" + height;
      window.open(url, 'sharer', sizeString);
    });
