development:
  rails:
    configuration:
      ruby_version: 2.0.0

production:
  rails:
    configuration:
      ruby_version: 2.0.0
      use_asset_pipeline: true
      do_initial_db_schema_load: true

  memcached:
    shared_group: redis
